# NetGuard

AI-powered security detection, analysis and remediation platform.

**Detect → Explain → Fix → Verify → Monitor**

> Work in progress. NetGuard is a *defensive* platform: only scan systems you own or are
> explicitly authorized to assess.

Full documentation is added as the platform is built (see `docs/` and the top-level guides).
