# Deliberately vulnerable fixture application

**Do not deploy.** These files exist only so NetGuard's scanners have known-bad code to detect
in automated tests. No real credentials are present; anything credential-like is a placeholder.
